meta {
  name: Get Product - Invalid UUID
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/products/:id
  body: none
  auth: none
}

params:path {
  id: not-a-valid-uuid
}

headers {
  Authorization: Bearer {{access_token}}
}

docs {
  Ожидаемый результат: 400 {"error": "Invalid UUID format"}
  Причина: uuid.Parse() вернул ошибку — валидация до вызова репозитория
}
