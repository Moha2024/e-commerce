meta {
  name: Register - Password Too Short
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "new-user@example.com",
    "password": "abc"
  }
}

docs {
  Ожидаемый результат: 422 {"error": "Invalid input: password must contain more than 6 characters"}
  Причина: len(password) < 6 — валидация в хендлере до вызова репозитория
}
