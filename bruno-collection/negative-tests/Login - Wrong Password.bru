meta {
  name: Login - Wrong Password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "negative-test@example.com",
    "password": "wrongpassword"
  }
}

docs {
  Ожидаемый результат: 401 {"error": "Invalid credentials"}
  Причина: bcrypt.CompareHashAndPassword вернул ошибку
  Намеренно совпадает с "Login - User Not Found" — нельзя раскрывать существует ли email
}
